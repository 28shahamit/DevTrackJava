plugins { id 'com.android.application' }

android {
    namespace 'com.javaroadmap.tracker'
    compileSdk 35

    defaultConfig {
        applicationId 'com.javaroadmap.tracker'
        minSdk 26
        targetSdk 35
        versionCode 4
        versionName '2.1.1'
    }
}

java {
    toolchain { languageVersion = JavaLanguageVersion.of(17) }
}
